create database dotnet_evaluation

create table student_table (register_no int not null primary key identity, st_name varchar(50), st_age int, st_phoneNum varchar(15), st_class int, st_photo varchar(200), st_email varchar(100), st_password varchar(50))

create table admin_table (admin_id int not null primary key identity, admin_email varchar(100), admin_password varchar(50))

create table marks_table (exam_id int not null primary key identity, st_regno int, math_marks int, english_marks int, java_marks int, cpp_marks int)
ALTER TABLE marks_table ADD total_marks int;

create table material_table (mat_id int not null primary key identity, mat_path varchar(150), mat_name varchar(100))